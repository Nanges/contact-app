import { SaveHandlerDirective } from './save-handler.directive';

describe('SaveHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
