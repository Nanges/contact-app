import { UseCancellationDirective } from './use-cancellation.directive';

describe('UseCancellationDirective', () => {
  it('should create an instance', () => {
    const directive = new UseCancellationDirective();
    expect(directive).toBeTruthy();
  });
});
