import { RemoveHandlerDirective } from './remove-handler.directive';

describe('RemoveHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
